message sample_message {
  oneof test_oneof { 
    string name = 1;
    int32 age = 2;
  }
 
  required string key = 3;
  optional int32 value = 4;

  oneof newtest {
    string bar = 5;
  }

  message user {
    required int32 id    = 1;
    optional string name = 2;
    optional sample_message msg =3;
  }

  repeated user users = 6;

}
